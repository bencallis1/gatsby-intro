import React form 'react'
import styled from "@emotion/styled"
import {css} from '@emotion/core'
import {Link} from 'gatsby'

const NavLink = styler(Link)`
  
`

const Header = () => (
  <header>
{ /* site name  */}
{ /* Navigation link  */}

  </header>

)